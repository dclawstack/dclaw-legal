apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-legal.fullname" . }}
  labels:
    {{- include "dclaw-legal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-legal.selectorLabels" . | nindent 4 }}
